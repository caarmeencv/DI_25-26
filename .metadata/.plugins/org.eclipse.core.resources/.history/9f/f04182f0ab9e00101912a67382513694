package com.jdojo.intro;

import javafx.application.Application;
import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.scene.control.TextField;
import javafx.scene.layout.VBox;
import javafx.stage.Stage;

public class MiniCuestionario extends Application {

	public static void main(String[] args) {
		Application.launch(args);
	}

	@Override
	public void start(Stage stage) throws Exception {

		VBox root = new VBox();

		// o enunciado do xogo
		Label enun = new Label("Cal é a capital de Galicia? \n - A Coruña");
		// as dimensions da nova xanela
		TextField resposta = new TextField();
		resposta.setPromptText("Introduce unha das opcions");
		// boton para enviar resposta
		Button btn = new Button("Enviar resposta");
		// funcion do boton para redimensionar
		btn.setOnAction(e -> {
			
		});

		root.getChildren().addAll(enun, resposta, btn);

		Scene scene = new Scene(root, 300, 200);
		stage.setScene(scene);
		stage.setTitle("Adiviña o numero");
		stage.show();

	}

}
